import { component$ } from "@builder.io/qwik"
import { CreatePenukaranComponent, Foot } from "../layout"

export default component$(() => {
  return (
    <>
      <CreatePenukaranComponent />
      <Foot />
    </>
  )
})
