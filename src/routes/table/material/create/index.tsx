import { component$ } from "@builder.io/qwik"
import { CreateIndexMaterial } from "~/components/page/material/form/create-index-material"

export default component$(() => {
  return <CreateIndexMaterial />
})
